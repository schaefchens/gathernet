{
  "name": "@gathernet/mls-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Typed facade over the mls-wasm module. Run `pnpm wasm:build` at the repo root before testing so wasm/ exists.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
